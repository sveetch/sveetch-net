"""
Settings for development environment
"""
from project.settings import *

DEBUG = TEMPLATE_DEBUG = True
ASSETS_DEBUG = True
INTERNAL_IPS = ()

# SMTP Settings to send Applications emails, configured for debug purpose only
# $> python -m smtpd -n -c DebuggingServer localhost:1025
EMAIL_HOST = 'localhost'
EMAIL_PORT = 1025
EMAIL_SUBJECT_PREFIX = '[${VARS:NAME} Dev] '
SERVER_EMAIL = '${VARS:NAME} errors <sveetch@gmail.com>'
DEFAULT_FROM_EMAIL = '${VARS:NAME} <sveetch@gmail.com>'